{
  "name": "mern-project",
  "version": "1.0.0",
  "description": "Full-Stack MERN (MongoDB, Express, React, Node.js) Application",
  "main": "server/src/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "start": "npm --prefix server start",
    "build": "cd client && npm install --include=dev && npm run build && cd .. && rm -rf dist public && cp -r client/dist dist && cp -r client/dist public",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "keywords": [
    "mern",
    "mongodb",
    "express",
    "react",
    "node"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vite": "^6.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
