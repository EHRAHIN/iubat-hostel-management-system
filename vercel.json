{
  "buildCommand": "cd client && npm install --include=dev && npm run build && cd .. && rm -rf public dist && cp -r client/dist public && cp -r client/dist dist",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
